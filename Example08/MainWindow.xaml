<Window x:Class="Example08.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Example08"
        mc:Ignorable="d"
        Title="Example 08" Height="450" Width="800">
    <Grid>
        <!--DataGrids, as the name suggests, are used to display data in a grid-style format.
        In this example, the DataGrid below is populated in the code-behind.

        By default, DataGrids will automatically generate its columns based on the properties
		publicly available for the objects it displays. In this case, this grid displays
        "Thing" objects, which is a class (wrongfully, but purposefully) defined in the code-behind.
        
        Check the code-behind to see how the grid below is being programatically manipulated.-->
		<DataGrid Name="myDataGrid" ColumnWidth="195" AutoGeneratedColumns="OnGenerateColumns"/>
	</Grid>
</Window>
